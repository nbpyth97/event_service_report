services:
  frontend:
    build: ./src/frontend
    container_name: meeting-scheduler-frontend-dev
    working_dir: /app
    volumes:
      - ./src/frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend
